on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read
  deployments: write

jobs:
  caling-deployment:
  uses: ./.github/workflows/deploy.yml
  with:
    deployment_environment: "DEV"
